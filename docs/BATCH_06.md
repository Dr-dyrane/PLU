# Batch 06: catalog remainder

Batch 06 accounts for the 175 normalized catalog rows not used by Batches 01–05. It deliberately separates catalog completeness from lesson readiness:

- 96 rows are ready lessons with exact source codes and visually reviewed recognition photos.
- 26 rows remain exact mapped references: 19 support source-row relationship lessons; 7 await media.
- 41 rows need source review and retain explicit reason codes.
- 12 rows remain catalog-only because they are outside the produce-learning scope.
- All 475 catalog IDs appear exactly once across the six batch manifests.

The first release admitted only Aloe (`3798`) and HH Red tomato (`4798`) from a 21-row strict loose-produce pool. The corrected pass follows the earlier batch method across the entire remainder: it searched all 163 in-scope identities, found 108 candidate sets, decoded 422 primary/alternative images, and pixel-reviewed every one. Seventy-five candidates passed visual review. Forty-one newly reviewed rows cleared the code and identity gates, joining a 12-row reviewed foundation for 53 lessons. A second pass admitted 22 more single-code rows by reusing already-reviewed recognition photos with explicit source/target provenance and label-assisted qualifier boundaries. At that stage, Batch 06 had 75 ready lessons and the full catalog had 375.

The subsequent 26-record recovery pass reviewed six inherited Batch 05 exclusions and twenty media gaps. It admitted all six exact workbook listings after recording row-specific adjudications, plus eleven media recoveries, reaching 392 playable lessons. Follow-ups add White sweet potato (`9925`) and Caribbean avocado (`58559`), bringing the catalog to 394. `data/batch-06-recovery-decisions.json` pins all 28 outcomes, their source evidence, and the exact media allowed to publish. The recovery files in `data/review-lanes/` preserve search attempts, rejected candidates, workbook locators, and rights findings.

The White sweet potato follow-up verifies `PLU Codes!A430:D430` against the available workbook and clears only that unflagged row's legacy exclusion. JanManu's `Sweet-potato.JPG` (CC BY-SA 3.0) explicitly identifies white-fleshed sweet potato in its source metadata. Pixel review confirms whole raw roots with pale tan skin, not an exposed white interior. The lesson therefore keeps White label-assisted, uses the established one-source photo-role fallback, and does not infer a named cultivar or store origin. The source sold-by cell remains blank; Weight/Loose remains an explicit interpretation. Neither the previous candidate rejection nor the missing-code Jamaican sweet-potato row is cleared by this photo reuse. The five original source photographs were not located in the repository, snapshot, or Downloads filename inventory, so handwritten and obscured rows still require the original images or new authoritative checkout evidence.

The 26 mapped references consist of seven single-code rows still missing usable recognition evidence, eleven same-label/different-code relationships, and eight shared-code relationships. All nineteen relationships have a dedicated compare → source-row recall → checkout-check flow, reached from home search, the Code relationships section, or its filter. The seven media cases are celery hearts, Goldendew, Honey Kiss, Meridol papaya, Taylor Gold pear, Fall Glo tangerine, and Mickey Lee watermelon. Their next steps require exact form/cultivar photographs with suitable rights; Meridol's candidate also shows a conflicting visible checkout code. They remain unfinished learning work.

The Caribbean avocado follow-up uses the unflagged `PLU Codes!A53:D53` listing. Franchesca TS's `Aguacates Dominicanos.jpg` (CC BY-SA 4.0) provides a photographer-identified Dominican example. Brooks Tropicals corroborates Caribbean category terminology, not the workbook product's cultivar, origin, brand, or supplier. The lesson shows whole green avocado forms, while Caribbean stays label-assisted and `58559` stays workbook-backed. The blank source sale unit is preserved; Weight/Loose remains interpreted. One reviewed source is explicitly reused across photo roles. `data/review-lanes/batch06-caribbean-avocado.json` records the evidence and limits.

The full-queue pass reviews every one of the remaining 50 records at revision 8df106d. Its three `data/review-lanes/batch06-final-*.json` ledgers preserve exact workbook locators, fresh photo searches, primary identity sources, rejected candidates, and specific missing evidence. All 41 queued records match the available workbook transcription, but Git refs/reflogs, the supplied snapshot, public GitHub releases, and targeted connected-Drive searches did not recover the original five reference photographs. Thirty-eight queued records still need original-sheet or current checkout evidence; the other three (Sour cabbage, Spice mango, Jamaican pumpkin) have clean code cells but no admitted identity/photo combination. Public PLU listings are not replacement store-code authority.

This pass admits Vanilla persimmon `69220` and Vanilla persimmon case `71207`, bringing the catalog to 396 checkout lessons. Primary nursery references link Vanilla to Vaniglia; De Pascale et al. (2023), Frontiers in Plant Science, Figure 1C provides the exact photographed cultivar under CC BY 4.0. A fixed SVG viewport displays only the reviewed panel of the unchanged source image, on both home cards and lesson stages. The source URL, SHA-256, dimensions, attribution, and panel bounds are pinned by `scripts/batch06-external-photo.mjs`; unavailable or changed source pixels fail generation. Product Story → Spot it displays author, publication, license, and one-source reuse disclosure. Vanilla and Case remain label-assisted; Weight/Loose versus Each/Case are explicit interpretations of blank source unit cells, not photographed store facts. Case quantity is not inferred.

The 41-row review queue has mutually exclusive next actions: 18 identity adjudications, nine missing-code captures, thirteen code reconciliations, and one long store-code classification. Thirty-four queued rows now retain reviewed candidate or reuse evidence; seven still need recognition media. Five newly reviewed candidates are Rainier cherries, cut jackfruit, Sweet Sop, purple-top white turnip, and walnuts. Their photos are recorded in the knowledge overlay, but `scripts/batch06-queued-media.mjs` and the build gate prohibit their promotion while source/code blockers remain. Baby bananas and frozen durian are context-only leads, not admitted recognition photos. The twelve non-produce rows remain catalog-only. There are 396 single-code lessons plus 19 relationship lessons. Forty-eight in-scope records still have no lesson (7 media gaps plus 41 queued); 67 still lack an unambiguous single-code lesson. Relationship study does not certify a current store checkout choice. The full research pass is complete; the full learning batch is not complete without the missing evidence.

`data/batch-06-relationship-decisions.json` pins these nineteen source-row contracts and their complete neighbors. Seventeen target rows recall one code with the source label/page visible; Gala apples require all three recorded codes (`4132`, `4133`, `4135`), and red-mango cases require both (`2146`, `13493`). Partial sets, repeated codes, invented codes, and dropped leading zeros are rejected. Neighbor arrays are never replaced with an earlier lesson's chosen primary code, and the handwritten mango neighbor stays queued. Photo-only selection and interchangeable-label claims fail the checkout check. Mapping-study progress is separate from learned checkout lessons.

`npm run prepare:relationships` resolves the 17 reviewed reference photographs into `data/relationship-lessons.json`, preserving author/license links. A fresh pixel review replaces the unavailable `Bosc Pear.jpg` reference with the exact `Bosc pear.jpg` source; its Large qualifier remains workbook-only. These are one-photo source-mapping studies, not three-view product-recognition mastery lessons. The separate old Dill cucumber lesson (`4596`) now uses raw `PicklingCucumbers.jpg`, replacing plated pickle imagery, with a durable generation override and Gruepig/public-domain provenance; its loose-produce code is unchanged.

The recovery's seventeen new lessons use 23 distinct reviewed photographs with author and license attribution. Blackberry, raspberry, and strawberry lessons use different images for hero, alternate, and context roles; cucumber and peeled garlic also gain a second view. Where only one appropriate photograph is available, the established seed fallback explicitly records its reuse across roles. Supplier and university identity references do not grant permission to copy their photographs.

Package, Organic, HH, FM, Jr, and similar store-label distinctions use a label-assisted contract. The photograph teaches only the visible produce identity, form, and color. The exact qualifier and code remain explicitly tied to the supplied workbook label; generated alt text and visual cues may not claim that those qualifiers are visible.

## Rebuild

```bash
npm run discover:batch06-media
python3 scripts/build-media-contact-sheets.py --report public/media-discovery-batch06.json
npm run compile:batch06-reviews
npm run generate:batch06
npm run audit:batch06-media
npm run prepare:relationships
npm run build
```

`data/batch-06-knowledge.json` is the claim-level truth overlay for all 175 rows. `data/batch-06-mapping-decisions.json` preserves the reviewed 61-row relationship set, while `data/batch-06-media-reuse.json` preserves the 44 reviewed source/target image relationships. `data/batch-06-media-review-decisions.json` and the original three review-lane files preserve the first complete pixel review. Recovery decisions can clear only the eight named legacy exclusions; handwritten flags, missing codes, and code collisions remain independent gates. `data/batch-06-reviewed-media.json` is the publishing ledger. `data/batch-06-dispositions.json` retains the complete admission outcome, while `data/batch-06-source.json` and `data/story-seeds/batch-06-generated.json` contain only ready rows.
